install the modules in both backend and front end
npm i
cd backend
npm i

Now that the dependencies are installed.

run this in the backend folder

npm run dev

run this in the weatherapprepo 
create new terminal which is in the weatherapprepo folder
npm start
